# Learnings index

Generated by `.claude/scripts/learnings_index.py` — do not edit by hand. Newest first.
Shown at every session start by `.claude/hooks/load-learnings.sh`.

- 2026-09-25 [A heredoc in a hook eats the payload, free private repos can't be protected, and /code-review is taken](2026-09-25-tooling-bootstrap-three-traps.md) — In a hook, capture stdin with `input=$(cat)` before any `python3 - <<'PY'`, because the heredoc *is* Python's stdin. Check that GitHub can protect a branch before you design gates around it. Give project commands names that no built-in uses.
- 2026-09-25 [A character-by-character tokenizer can keep raw offsets and still equal the whole-string definition](2026-09-25-tokenizer-char-by-char-matches-whole-string.md) — Pin an offset-tracking implementation to the simplest whole-string statement of the same rule with a Hypothesis property over an adversarial alphabet. Without that property, NFKC/casefold/NFD differences between one character and a sequence go unnoticed.
- 2026-09-25 [The first gates passed every test and still let `git status;git push` through](2026-09-25-gates-passed-their-own-tests-and-missed-real-commands.md) — A gate's test table proves nothing until each row is shown to fail against a deliberately broken gate. Parse commands the way bash does (unspaced separators, newlines, heredocs, prefixes, redirects), not with `shlex.split`.
- 2026-09-25 [`gh pr edit` fails on this GitHub, so the review attestation goes through the REST API](2026-09-25-first-merge-gh-pr-edit-and-scripted-backlog.md) — Use `gh api -X PATCH repos/{owner}/{repo}/pulls/<n>` to change a PR body, because `gh pr edit` fails on the retired Projects (classic) API. Merge PRs whose learnings or dispositions cite commit SHAs with a merge commit, not a squash. Build a large backlog from a script that checks dependency order before it creates anything.
- 2026-09-25 [A workspace member synced before its source existed installs as an empty package](2026-09-25-backend-skeleton-empty-editable-install.md) — If `uv sync` runs while a workspace member's `src/` is still empty, the editable install records nothing and imports fail even after you add the code. Run `uv sync --reinstall-package <name>` once the package has source files.
