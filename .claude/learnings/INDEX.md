# Learnings index

Generated by `.claude/scripts/learnings_index.py` — do not edit by hand. Newest first.
Shown at every session start by `.claude/hooks/load-learnings.sh`.

- 2026-09-25 [A heredoc in a hook eats the payload, free private repos can't be protected, and /code-review is taken](2026-09-25-tooling-bootstrap-three-traps.md) — In a hook, capture stdin with `input=$(cat)` before any `python3 - <<'PY'`, because the heredoc *is* Python's stdin. Check that GitHub can protect a branch before you design gates around it. Give project commands names that no built-in uses.
